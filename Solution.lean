import FloorSaving

/-!
`Solution` is the module named in `comparator.json`.

The declarations checked by comparator are imported from the `FloorSaving`
library:

* `FloorSaving.floor_saving_lower_bound`
* `FloorSaving.endpoint_limsup`
-/
